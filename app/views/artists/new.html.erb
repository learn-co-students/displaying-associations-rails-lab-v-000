<h1>Create an Artist</h1>

<%= form_for @artist  do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <br />
  <%= f.collection_check_boxes :song_ids, Song.all, :id, :title%>
  <%= f.submit %>
<% end %>
