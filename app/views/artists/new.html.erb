<h1>Create an Artist</h1>

<%= form_for @artist  do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <br />

  <%= f.submit %>
<% end %>

<ul>
<% @artist.songs.each do |song| %>
  <li>
    <%= link_to song.title, song_path(song) if song %>
  </li>
<% end %>
</ul>
