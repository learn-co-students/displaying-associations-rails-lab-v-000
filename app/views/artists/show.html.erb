<h1><%= @artist.name %></h1>
<h3><%= @artist.song_count %> song(s) %></h3>
<%= link_to @artist.name, artist_path(@artist) %>
<ul>
	<% @artist.songs.each do |song| %>
		<li><%= link_to song.title, song_path(song) %></li>
	<% end %>
</ul>