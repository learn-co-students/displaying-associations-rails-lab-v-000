<%= @artist.name %><br>
<h2>Song Count:</h2>
<p><%= @artist.song_count %> songs </p><br>
<ul>
  <% @artist.songs.each do |song| %>
    <li><%= link_to song.title, song_path(song) %></li>
  <% end %>
</ul>
