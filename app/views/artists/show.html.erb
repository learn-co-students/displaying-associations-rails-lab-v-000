<h1><%= @artist.name %></h1>

<h3><%= pluralize(@artist.songs.count, 'Song') %></h3>
<h3><%= @artist.songs.count %>

<ul>
  <% @artist.songs.each do |p| %>
    <li><%= link_to p.title, song_path(p) %></li>
  <% end %>
</ul>
