<h1>Artist Name</h1>

<p><%= @artist.name %></p>

<ul>
  <% @artist.songs.each do |song| %>
  <li><%= link_to song.title, song_path(song) %> </li>
  <% end %>
</ul>


<%= link_to 'Index', artists_path %>
