<h1><%= @artist.name %> has <%= @artist.song_count %></h1><br>
<ul>
  <%= div_for(@artist.songs, class: "song-show-page") do |song| %>
    <li><%= link_to song.title, song_path(song) %></li>
  <% end %>
</ul>
