<!-- <h1><%= @artist.name %> </h1> -->
<ul>
  <%= pluralize(@artist.songs.count, 'song') if @artist.songs.any? %>
<% @artist.songs.each do |song| %>
<li><%= link_to song.title, song_path(song) %></li>
<% end %>
</ul>
