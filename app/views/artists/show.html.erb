<h1> <%= @artist.name %> </h1>

<h2> <%= pluralize(@artist.song_count, "Song") %> </h2>
  <ul>
    <% @artist.songs.each do |song| %>
      <h3><li> <%= link_to(song.title, song_path(song)) %> </li></h3>
    <% end %>
  </ul>
