<h3><%= @artist.name %></h3>
<p>Current number of songs: <%= "#{@artist.song_count} songs" %></p>

<ul>
  <% @artist.songs.each do |song| %>
  <li><%= link_to song.title, song_path(song) %></li>
  <% end %>
</ul>
