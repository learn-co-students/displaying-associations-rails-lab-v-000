<h2>Show page for <%= @artist.name %></h2>

<h3>A list of songs belonging to <%= @artist.name %></h3>
  <ul>
  <% @artist_songs.each do |s| %>
    <li> <%= link_to s.title, song_path(s)%></li>
  <% end %>
  </ul>
