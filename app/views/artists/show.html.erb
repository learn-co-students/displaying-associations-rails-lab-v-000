<h1> <%= @artist.name %> </h1>

<h2> <%= pluralize(@artist.song_count, "Song") %> </h2>

  <% @artist.songs.each do |song| %>
    <h3><p> <%= link_to(song.title, song_path(song)) %> </p></h3>
  <% end %>
