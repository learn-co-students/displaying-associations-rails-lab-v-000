<% if @artist.songs.any? %>

  <h3><%= pluralize(@artist.song_count, 'song') + ":" %></h3>

  <ul>

    <% @artist.songs.each do |song| %>

      <li><%= link_to song.title, song %></li>

    <% end %>

  </ul>

<% end %>