<!-- SHOW -->
<h1><%= @artist.name %></h1>

<p>Songs:</p>
<ul>
  <% @artist.songs.each do |song| %> <!-- CAN I REFACTOR THIS? -->
    <li><%= link_to song.title, song_path(song) %></li>
  <% end %>  
</ul>
