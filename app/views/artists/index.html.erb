<% @artists.each do |artist| %>
  <h2><%= artist.name %></h2>
  <h3><%= artist.song_count %> songs</h3>
  <ul>
    <% artist.songs.each do |song| %>
      <li><%= song.title %></li>
    <% end %>
  </ul>
<% end %>
