<h1>Artist Index</h1>
<% @artists.each do |artist| %>
  <%= artist.name %>
  <p><%= pluralize(artist.song_count, 'song') %></p>
  <% artist.songs.each do |song| %>
    <ul>
      <li><%= song.title %></li>
    </ul>
  <% end %>
<% end %>
