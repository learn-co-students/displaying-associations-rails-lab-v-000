<% @artists.each do |artist| %>
      <h1><%=artist.name%><br></h1>
      <h3><%artist.songs.each do |song| %>
        <%=link_to(song.title, song_path(song))%><br>
        <%end%></h3>
      <h1> <%=artist.name%> has <%=artist.songs.size%> song(s)</h1>
      <%end%>
