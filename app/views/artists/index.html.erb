<h1>View all Artists</h1>

<ul>
    <% @artists.each do |artist| %>
        <li>
        <h4><%= link_to artist.name, artist_path(artist)%></h4>
        <h4>(<%= artist.song_count %> <%= "song".pluralize(artist.song_count) %> )</h4>
        </li>
    <% end %>
</ul>