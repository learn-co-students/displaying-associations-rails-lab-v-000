<h1>Artists</h1>

<ul class="artist">
  <% @artist.each do |artist| %>
    <li><%= link_to artist.name, artist_path(artist) %></li>
    <li><%= artist.song_count %> songs</li>
    <br>
  <% end %>
</ul>
