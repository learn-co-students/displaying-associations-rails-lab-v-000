<%# artists/index.html.erb %>
<h1>All Artists</h1>

<ul>
  <% @artists.each do |a| %>

    <li>
      <%= link_to a.name, artist_path(a) %> | <%= a.song_count %> <%= pluralize(a.song_count, 'song') %>
    </li>

  <% end %>
</ul>
