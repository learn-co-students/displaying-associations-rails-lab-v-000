<%= @artists.each do |artist| %>
    <h1><%= artist.name %></h1>
    <h2><%= pluralize(artist.songs.count, 'song') %></h2>
    <ul>
      <% artist.songs.each do |song| %>
        <li><%= song.title %></li>
      <% end %>
    </ul>
<% end %>
