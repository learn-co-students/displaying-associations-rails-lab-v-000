<h1>Artists:</h1>

<ul>
  <h3><% @artists.each do |artist| %>
    <li><%= link_to artist.name, artist_path(artist) %></li>
    <%= artist.song_count %> <%= "song".pluralize(artist.song_count) %>
  <% end %>
</ul>
