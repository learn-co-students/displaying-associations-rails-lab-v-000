<% @artists.each do |artist| %>
  <h3><%= link_to artist.name, artist_path(artist) %> - <%= artist.song_count %> songs</h3><br>

  <ul>
    <li>
      <% artist.songs.each do |song| %>
        <%= link_to song.title, song_path(song) %>
      <% end %>
    </li>
  </ul>
<% end %>
