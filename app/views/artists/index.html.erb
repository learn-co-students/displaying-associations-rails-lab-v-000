<%@artists.each do |artist|%>
  <h2><%=link_to artist.name, artist_path(artist)%></h2><br>
  <p><%=artist.song_count.to_s + " song(s):"%></p><br>
  <ul>
  <%artist.songs.each do |song| %>
    <li><%= link_to song.title, song_path(song) %></li>
  <% end %>
</ul>
<%end%>