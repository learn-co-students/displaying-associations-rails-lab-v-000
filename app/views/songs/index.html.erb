<h2>All songs</h2>

<ul>
	<%= @songs.each do |song|%>
	  <% html_text = song.artist.name + ' - ' + song.title %>
	  <li><%= link_to html_text, song_path(song.id) if song.artist %></li>
	<% end%>
</ul>