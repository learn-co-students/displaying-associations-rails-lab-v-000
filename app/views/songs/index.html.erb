<h1> Song Index </h1>
<h3> <%= pluralize(@songs.count, "Song")%> </h3>
    <ul>
    <% @songs.each do |song| %>
        <li> <%= link_to "#{song.artist_name} - #{song.title}", song_path(song) %> </li>
        <% end %>
    </ul>