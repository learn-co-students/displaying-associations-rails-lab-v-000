<h1>All Songs</h1>

<ul>
  <% @songs.each do |song| %>
    <%# <li><%= link_to song.artist_name + "-" + song.title, song_path(song) %>
     <%# <%= link_to song.title, song_path(song) %>
     <a href="<%=song_path(song)%>"><%= song.artist_name %> - <%= song.title %></a>
  <%end%>
</ul>


<%# <a href="<%= song_path(song) %>
        <%# <%= song.artist_name %>
        <%# <%= song.title %>
      <%# </a>  %>