<p><%= link_to 'artist list', artists_path %></p>
<h1>Songs</h1>
<%= button_to "Create Song", new_artist_path, method: :get %><br>
  <% @songs.sort_by{|s| s.artist_name}.each do |song| %>
    <%= link_to song.artist_name+" - "+song.title, song_path(song) %>
  <% end %>
