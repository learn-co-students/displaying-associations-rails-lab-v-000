<h1> Songs Index </h1>
<% @songs.each do |s| %>
<li><%= link_to "#{s.artist_name} - #{s.title}", song_path(s) %></li>
<li><%= link_to "#{s.artist_name} - #{s.title}", artist_path(s.artist_name) %></li>
<% end %>
