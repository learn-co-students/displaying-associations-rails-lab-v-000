<h1>Songs</h1>

<ul>
  <% @songs.each do |song| %>
    <li>
      <%= link_to song.artist_name, artist_path(song.artist) if song.artist %>
        -
      <%= link_to song.title, song_path(song) %>
    </li>
  <% end %>
</ul>
