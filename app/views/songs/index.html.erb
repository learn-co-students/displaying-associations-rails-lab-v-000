<h1>Available Songs:</h1>

<ul>
    <% @songs.each do |song| %>
    <!-- <li><%= link_to song.artist_name, artist_path(song.artist_id) %> - <%= link_to song.title, song_path(song) %></li> -->
    <li><%= link_to "#{song.artist_name} - #{song.title}", song_path(song) %></li>

    <% end %>
</ul>