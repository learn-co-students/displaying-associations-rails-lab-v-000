<h1>Songs</h1>

<ul>
  <% @songs.each do |song| %>
    <li>
      <a href="<%= song_path(song) %>">
        <%= song.artist_name %>
        -
        <%= song.title %>
      </a>
      <%= link_to song.artist, artist_path(song.artist)%>
    </li>
  <% end %>
</ul>
