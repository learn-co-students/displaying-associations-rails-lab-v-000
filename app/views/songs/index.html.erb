<h1>Welcome to our songs page!</h1>
<ul>
<%= @songs.each do |song| %>
	<li><%= link_to song.artist.name, artist_path(song.artist) %>
	<ul>
		<li><%= link_to "#{song.artist.name} - #{song.title}", song_path(song) %></li>
	</ul>
<% end %>
</ul>
