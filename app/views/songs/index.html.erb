<h1>Songs</h1>
<% @songs.each do |song| %>
    <%= link_to song.artist_name + " - " +  song.title, song_path(song)rial %><br>
    <%= link_to song.artist_name, artist_path(song.artist) %><br>
<% end %>