<h1>Songs</h1>
<ul>
  <% @songs.each do |song| %>
      <li><%= link_to song.artist_title, song_path(song) %></li>
      <li><%= link_to song.artist, artist_path(song.artist) %></li>
  <% end %>
</ul>
