<h1>Here are the Songs:</h1>

<% @songs.each do |song| %>
  <p> <%= link_to song.format_artist_name_title, song_path(song) %> </p>
  <p><%= link_to song.artist.name, artist_path(song.artist) %></p>
<% end %>
