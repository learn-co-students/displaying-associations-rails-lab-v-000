<h1><%= pluralize(@songs.count, 'Song')%>:</h1>
<ul>
  <% @songs.each do |s| %>
    <li><%= link_to song_path(s) do %>
      <%= s.artist_name %> - <%= s.title %>
    <% end %></li>
  <% end %>
</ul>
