<h2>Songs</h2>

<ul>
  <% @songs.sort_by{|s| [s.artist_name, s.title]}.each do |s| %>
    <li>
      <a href="<%= song_path(s) %>"><%= s.artist_name %> - <%= s.title %></a>
    </li>
  <% end %>
</ul>
