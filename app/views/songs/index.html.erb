<h1>Songs</h1>

<% @songs.each do |song| %>
<%= link_to "#{song.artist_name} - #{song.title}", song_path(song.id) %>
<!-- <a href="<%= song_path(song) %>">
     <%= song.artist_name %>
     -
     <%= song.title %>
   </a> -->
<% end %>
