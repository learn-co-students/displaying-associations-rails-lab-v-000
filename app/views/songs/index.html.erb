<h1>Songs</h1>

<ul>
	<% @songs.each do |song| %>
		<li><%= link_to "#{song.artist_name} - #{song.title}", song_path(song) %> <%= link_to song.artist_name, artist_path(song.artist) %></li>
	<% end %>
</ul>
