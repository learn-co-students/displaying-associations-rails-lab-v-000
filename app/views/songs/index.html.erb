<h1>Here's some songs!</h1>

<% @songs.each do |song| %>
  <% @fluff = song.artist_name  + " - " + song.title %> 
    <%= link_to @fluff, song_path(song) %><br>
  <%= link_to song.artist_name, artist_path(song.artist) %>
<% end %>

