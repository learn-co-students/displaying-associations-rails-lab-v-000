<h2>Songs</h2>

<ul>
<% @songs.each do |song| %>
  <li>
    <% artist_song = song.artist_name.titlecase + ' - ' + song.title.titlecase %>
    <%= link_to artist_song, song_path(song) %>
  </li>
<% end %>
</ul>
