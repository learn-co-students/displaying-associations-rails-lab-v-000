<ul>
<% @songs.each do |song| %>
<li>
  <a href="<%= song_path(song) %>">
    <%= song.artist_name %> - <%= song.title %>
  </a>
  <br>

  <%= link_to song.artist_name, artist_path(song.artist) %>

</li>
<% end %>
</ul>


